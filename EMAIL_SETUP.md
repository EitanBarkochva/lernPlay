# שליחת דוח במייל

בכל דוח (סיום משחק / סיום מבחן) יש עכשיו פאנל **📧 שליחת הדוח במייל**, עם שתי דרכים:

## 1. 📧 פתיחת אפליקציית המייל (mailto) — עובד מיד, בלי הגדרות
לוחצים על **"פתח באפליקציית המייל"** → נפתחת אפליקציית המייל שלך (Gmail/Outlook/וכו')
עם הנמען, הנושא והדוח **מוכנים** — רק לוחצים שלח. המייל נשלח **מהחשבון שלך**.
זו הדרך המומלצת — אין מה להגדיר.

## 2. 🚀 שליחה אוטומטית אמיתית (Resend) — אופציונלי
שולח את הדוח אוטומטית בלי לפתוח אפליקציית מייל. דורש הגדרה חד-פעמית:

### שלב א — חשבון Resend
1. היכנס ל-https://resend.com והירשם (חינם, עד 100 מיילים ביום / 3,000 בחודש).
2. **API Keys → Create API Key**, העתק את המפתח (`re_...`).

### שלב ב — הגדרת הסוד ב-Supabase
1. https://supabase.com/dashboard/project/tgmafboqaezfleggttyk/functions/secrets
2. הוסף סוד: **Name:** `RESEND_API_KEY` · **Value:** המפתח (`re_...`)
3. (אופציונלי) `RESEND_FROM` = כתובת השולח (ברירת מחדל `onboarding@resend.dev`)

### שלב ג — שליחה לכל כתובת (אימות דומיין)
במצב החינמי הבסיסי של Resend אפשר לשלוח **רק לכתובת שאיתה נרשמת**.
כדי לשלוח לכל כתובת הורה — צריך **לאמת דומיין** ב-Resend:
**Domains → Add Domain** → הוסף רשומות DNS (Resend מנחה אותך) → ואז הגדר
`RESEND_FROM` לכתובת בדומיין שלך (למשל `reports@yourschool.co.il`).

> בלי אימות דומיין — השליחה האוטומטית תעבוד רק לכתובת שלך. **לכן ל-mailto
> אין מגבלה כזו** והוא הדרך הפשוטה לשלוח לכל הורה.

## איך זה עובד מאחורי הקלעים
- `send-report` (Edge Function) שולח דרך Resend; המפתח נשמר בצד השרת בלבד.
- אם `RESEND_API_KEY` לא מוגדר — הכפתור מציג הודעה ידידותית ומפנה ל-mailto.
