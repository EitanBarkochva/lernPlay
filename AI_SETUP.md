# יצירת שאלות אוטומטית ב-AI — הגדרה

המערכת מחוברת ל-Claude API דרך **Edge Function** מאובטח ב-Supabase
(`generate-questions`). המפתח הסודי נשמר בצד השרת — לא בקוד ולא בדפדפן.

נשאר שלב אחד חד-פעמי: להוסיף את מפתח ה-API שלך.

## שלב 1 — צור מפתח API ב-Anthropic
1. היכנס ל-https://console.anthropic.com
2. הירשם (יש קרדיט חינמי להתחלה).
3. **Settings → API Keys → Create Key**, העתק את המפתח (מתחיל ב-`sk-ant-...`).
4. מומלץ: **Settings → Limits** — הגדר תקרת הוצאה חודשית (למשל $5) כדי שלא תופתע.

## שלב 2 — הגדר את המפתח כסוד ב-Supabase
דרך לוח הבקרה:
1. היכנס לפרויקט **lernPlay** ב-https://supabase.com
2. **Project Settings → Edge Functions → Secrets** (או **Edge Functions → Manage secrets**)
3. הוסף סוד חדש:
   - **Name:** `ANTHROPIC_API_KEY`
   - **Value:** המפתח שהעתקת (`sk-ant-...`)
4. שמור. זהו!

> אפשר גם דרך ה-CLI: `supabase secrets set ANTHROPIC_API_KEY=sk-ant-...`

## שלב 3 — להשתמש
1. פתח את האתר → **יצירת משחק חדש** → מלא כיתה/מקצוע/נושא → המשך.
2. במסך הוספת השאלות, בכרטיס הסגול **"🤖 יצירת שאלות אוטומטית ב-AI"**:
   בחר כמה שאלות → **✨ צור שאלות ב-AI**.
3. השאלות ייווצרו ויתווספו לרשימה. אפשר לערוך, להוסיף עוד, ולסיים כרגיל.

## עלות ומודל
- ברירת המחדל: **Claude Sonnet 4.6** (איכותי, ~5–7 סנט ל-30 שאלות).
- רוצה את האיכות המקסימלית? הוסף סוד נוסף `ANTHROPIC_MODEL` עם הערך
  `claude-opus-4-8` (יקר יותר אך חזק יותר).
- העלות מחויבת רק כשמשתמשים, ומכוסה לרוב ע"י הקרדיט החינמי בהתחלה.

## בעיות נפוצות
- **"חסר מפתח ANTHROPIC_API_KEY"** → לא הוגדר הסוד (שלב 2), או שצריך לרענן
  (לפעמים לוקח רגע עד שהסוד נטען לפונקציה).
- **"Anthropic 401"** → המפתח שגוי/לא פעיל. בדוק שהעתקת נכון.
- **"Anthropic 400"** → בדוק שהמודל ב-`ANTHROPIC_MODEL` תקין (אם הגדרת).
